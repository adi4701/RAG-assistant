# ── Required ──────────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-...

# ── Security (change before deploying!) ───────────────────────────────────────
SECRET_KEY=lexrag-super-secret-32-char-key-here

# ── Services (Docker defaults — change if running locally without Docker) ─────
REDIS_URL=redis://redis:6379
CHROMA_HOST=chromadb
CHROMA_PORT=8000

# ── Tuning (paper-optimal defaults) ───────────────────────────────────────────
CHUNK_SIZE=1000
CHUNK_OVERLAP=150
TOP_K=5
COSINE_THRESHOLD=0.75
CACHE_TTL=3600
